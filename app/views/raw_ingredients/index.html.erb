<p id="notice"><%= notice %></p>

<h1>Raw Ingredients</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @raw_ingredients.each do |raw_ingredient| %>
      <tr>
        <td><%= link_to 'Show', raw_ingredient %></td>
        <td><%= link_to 'Edit', edit_raw_ingredient_path(raw_ingredient) %></td>
        <td><%= link_to 'Destroy', raw_ingredient, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Raw Ingredient', new_raw_ingredient_path %>
